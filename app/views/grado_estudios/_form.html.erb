<%= form_for(@grado_estudio) do |f| %>
  <% if @grado_estudio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grado_estudio.errors.count, "error") %> prohibited this grado_estudio from being saved:</h2>

      <ul>
      <% @grado_estudio.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="field">
  <%= f.label :nombre, "Grado de estudios"%>
  <%= f.text_field :nombre %>
</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
